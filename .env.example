# MongoDB connection string for the backend server (server/index.js)
MONGODB_URI="mongodb://localhost:27017/gctu_navigator"

# Port for the Express API server
PORT=5000

# Admin credentials for the minimal admin auth flow
# ⚠️ Change these before running in production — do not use defaults as-is
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
JWT_SECRET=change-me-in-production
